% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tetmer.R
\name{expectedSpectrum}
\alias{expectedSpectrum}
\alias{expectedSpectrum,tetmerFit-method}
\alias{expectedSpectrum,character-method}
\title{Build an expected spectrum from a model}
\usage{
expectedSpectrum(object, ...)

\S4method{expectedSpectrum}{tetmerFit}(
  object,
  xrange = object@xrange,
  name = object@model,
  k = object@k
)

\S4method{expectedSpectrum}{character}(object, par, xrange, name = object, k = 0)
}
\arguments{
\item{object}{Either a \code{tetmerFit} object or a model code such as
\code{"d"} or \code{"tal"}.}

\item{...}{Additional arguments passed to methods.}

\item{xrange}{Numeric vector of length 2 giving the multiplicity range
for the generated spectrum. For the \code{tetmerFit} method, defaults
to \code{object@xrange}. For the \code{character} method, this must
be supplied explicitly.}

\item{name}{(optional) Name for the returned \code{spectrum}. Defaults to
the fit model or model code if omitted.}

\item{k}{(optional) k-mer length for the returned \code{spectrum}. For
the \code{tetmerFit} method, defaults to \code{object@k}. For the
\code{character} method, defaults to \code{0}.}

\item{par}{For the \code{character} method, a named numeric vector of
model parameters. Required names are \code{cov}, \code{vf},
\code{theta}, and \code{haplSize}, plus \code{diverg} for
allopolyploid models and \code{pallo} for \code{"tse"}.}
}
\value{
A \code{spectrum} object containing expected counts for the
specified model.
}
\description{
Generates a synthetic \code{spectrum} object representing the expected
k-mer counts under one of Tetmer's models.
}
\examples{
\dontrun{
fit <- fitSpectrum(E030, model = "d", kcov = c(5, 100), vf = c(1, 30),
                   log10theta = c(-3, 0), log10Mbp = c(0, 3),
                   xrange = c(45, 200),
                   maxAttempts = 1)
expectedSpectrum(fit)
expectedSpectrum("d",
                 par = c(cov = 30, vf = 1.5, theta = 0.04, haplSize = 200),
                 xrange = c(10, 80),
                 k = 21)
}
}
